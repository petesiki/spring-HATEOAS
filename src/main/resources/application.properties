# Application properties
server.port=8080

# Trust all proxies - in production, restrict to trusted proxy IPs
server.forward-headers-strategy=NATIVE

# Custom header configuration - change these to match the headers your proxy uses
custom.header.host=X-Custom-Host
custom.header.proto=X-Custom-Proto
custom.header.port=X-Custom-Port